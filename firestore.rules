rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Deny all reads and writes by default.
    // Specific rules for collections will be added below.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
